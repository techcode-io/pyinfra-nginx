# Set real client IP from Cloudflare
# Ref: https://www.cloudflare.com/ips/
set_real_ip_from {{ cloudflare_ipv4_ranges | join(";\nset_real_ip_from ") }};

set_real_ip_from {{ cloudflare_ipv6_ranges | join(";\nset_real_ip_from ") }};

real_ip_header CF-Connecting-IP;
real_ip_recursive on;
